# 生产环境变量模板：复制为 .env 并填写
# 生成强随机密钥（≥32 字节）：python3 -c "import secrets; print(secrets.token_hex(32))"

# 部署环境：dev（开发，弱密钥允许）/ prod（生产，JWT_SECRET 强制 ≥32 字节否则拒绝启动）
ENVIRONMENT=prod

JWT_SECRET=change-me-to-random-hex
